import IO

for x in range(0, 10, 2) {
    IO.puts("step(2): ", x)
}
IO.puts("-----------------")

for x in range(0, 10, 3) {
    IO.puts("step(3): ", x)
}
IO.puts("-----------------")
for x in range(0, 10) {
    IO.puts("step(normal): ", x)
}
