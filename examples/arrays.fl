import io

define main(): void {
  let nums = [34, 35, 69, 420]
  let fruits = ["Apple", "Orange"]
  let points: Array<float> = []

  io.puts("Nums: ", nums)

  io.puts("Fruits:")
  for f in fruits {
    io.puts(f)
  }

  io.puts("Points: ", points)
}

main()
